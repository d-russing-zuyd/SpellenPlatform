@model IEnumerable<SpellenPlatform.Models.Category>

<ul class="navbar-nav mr-auto">
    @foreach (var category in Model)
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Games" asp-action="@category.CategoryName">@category.CategoryName</a>
        </li>
    }
</ul>
